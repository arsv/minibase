Some documentation on the project as a whole.

    initrd.txt      Booting with initrd
    startup.txt     System startup sequence (past initrd)
    login.txt       Re-thinking login
    dcrypt.txt      Disk encryption
    console.txt     Setting up virtual consoles
    shell.txt       The three kinds of shells the system needs

    control.txt     Runtime control for the services
    baselib.txt     The base library used in the project

For design notes on individual tools, check ../src directories.
