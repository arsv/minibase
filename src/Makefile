include ../config.mk

/ = ../

bin = basename cat chmod chown chvt clear df dirname dmesg du echo errno \
	false hello hexdump kill mkdir mknod pwd ren rm sleep strings sync \
	sysinfo tee touch true uname uptime wc which write xargs yes symlink

sbin = fstrim insmod mount mountvfs rmmod switchroot umount systime

include ../common.mk
